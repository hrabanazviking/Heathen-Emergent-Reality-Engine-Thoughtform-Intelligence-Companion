# Volume 6 — Security, Safety, and Reliability Spec

## Threat Model Matrix
## Threat Class 1
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 2
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 3
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 4
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 5
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 6
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 7
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 8
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 9
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 10
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 11
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 12
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 13
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 14
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 15
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 16
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 17
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 18
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 19
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 20
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 21
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 22
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 23
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 24
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Threat Class 25
- Attack surface: user input, tool output, retrieval corpus, plugin boundaries, model routing metadata.
- Controls: input canonicalization, instruction hierarchy lock, signed tool responses, policy post-checks.
- Detection: anomaly scoring, intent mismatch alerts, policy violation counters.
- Recovery: isolate tenant/session, rotate credentials, replay forensic bundle, notify on-call.

## Secure Coding Reference Snippet
```python
def enforce_tool_policy(tool_name: str, user_scope: set[str], allowed: dict[str, set[str]]) -> bool:
    if tool_name not in allowed:
        return False
    return len(user_scope & allowed[tool_name]) > 0
```